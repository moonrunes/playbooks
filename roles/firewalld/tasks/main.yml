  - name: Ensure firewalld is running.
    tags:
    - start
    - firewall
    become: true
    become_method: doas
    ansible.builtin.service:
      name: firewalld
      state: started
      enabled: true
