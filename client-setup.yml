  - name: Client setup playbook
    hosts: client
    remote_user: nick
    gather_facts: yes
#   vars_files: ./group_vars/variables.yml
    roles:
      - auditd
      - clamav
      - firewalld
      - flatpak
      - git
      - lynis
      - rkhunter
      - tailscale